# CookieCutter

## A Docker image for `cookiecutter`

This repository provides an automated build for a lean [CookieCutter](https://cookiecutter.readthedocs.io) Docker image.

## Usage


```
docker run --rm -ti -v $(pwd):/mnt alastairhm/cookiecutter <Options>
```
